interface:
  display_name: "CxImage"
  short_description: "Repo-specific patterns and workflows for CxImage"
  default_prompt: "Use the CxImage repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true